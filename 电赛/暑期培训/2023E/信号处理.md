
以5kHz步进，采样率选为320kHz,不会发生频谱泄漏

通过排列法，可以挑选出两个最大的幅度值

但是两个最大幅度值可能导致 A,B 与 A',B' 顺序交换。


因此，需要做一个假设：$f_{0} < f_{1}$, 保证A的输入信号频率小于B

难点在于，当A,B的高次谐波发生频谱混叠


软件实现比较器？（ADC的输入捕获功能？）
![[Pasted image 20250715103529.png]]
在单片机中，将AD采样频率设置为1MHz,采集1000个点存于数组中，从-1V到1V，以0.05V为步进，设置比较门限

$$
1000 \times 10^{-6} = 10^{-3}s = 1ms
$$
10kHz:一个周期采样10个点
$$
T = \frac{1}{10^{4}} = 0.1ms
$$

100kHz：一个周期采样100个点
$$
T = \frac{1}{10^{5}} = 0.01ms
$$




题目要求A’和A稳定显示不漂移 由于数字方案产生的A’是用DAC生成的，不是从A+B中分离出来的，所以不同与A不同源，所以**如果不干预A’，A’就会慢慢地在示波器上朝一个方向漂移，其原因是生成的A’与A的频率有固定的差值f**，f的绝对值虽然很小，稍微过几个周期肉眼和示波器上都看不出来的，但是这个误差不断累计，就会肉眼可见，示波器上自然也漂移了。 所以**要在示波器发生肉眼可见的漂移前对波形就行一次刷新**，从而使误差从新开始累计，从而使得示波器上A与A’虽然不完全一样，但是相似度非常高，肉眼无法分辨，示波器上的表现就是稳定不漂移。 **我们队伍的刷新方法就是，确定1ms（对应分辨率1kHz）内A+B波形通过比较器后上升沿个数为x**，FPGA每x个上升沿，使DAC输出从0相位开始输出（用单片机就相当于重新开始一次DAC的DMA数组输出）。


关键是如何判断正弦波和三角波？




| 波形  | $f_{0}$ | $f_{1}$ | 异常                                                  |
| --- | ------- | ------- | --------------------------------------------------- |
| 正弦  | 10kHz   | 70kHz   | 测量频率sig1:90kHz,sig2: 70kHz,<br>type1 = 1, type2 = 0 |
|     | 10kHz   | 80kHz   | 测量频率sig1:80kHz,sig2: 80kHz,                         |
