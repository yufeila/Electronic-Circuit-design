
## 总体文件架构

```
ProjectRoot/
├── Core/
│   ├── Inc/
│   │   ├── main.h
│   │   ├── stm32xxxx_hal_conf.h
│   │   ├── stm32xxxx_it.h
│   │   └── ...
│   └── Src/
│       ├── main.c
│       ├── stm32xxxx_it.c
│       ├── stm32xxxx_hal_msp.c
│       └── ...
├── Drivers/
│   ├── CMSIS/
│   │   └── ...（ARM 内核和设备头文件）
│   └── STM32xxxx_HAL_Driver/
│       ├── Inc/
│       │   └── ...（HAL 库头文件）
│       └── Src/
│           └── ...（HAL 库源文件）
├── Middlewares/         （有些中间件会生成，比如FATFS、USB等）
├── MX_XXX.ioc           （CubeMX的工程配置文件）
├── .project/.cproject   （有时生成，IDE工程文件）
├── Makefile/Keil工程/…  （与你选择的工具链有关）
└── README.md            （有时生成）

```

当在CubeMx中勾选为每个外设单独生成.c和.h文件之后，不同外设的.c和.h文件会生成在Core下的Inc和Src下面

## 中断调用路径

以DMA为例

CubeMx会专门生成stm32f103xx_it.c文件，里面存放着中断函数， 这里的中断函数可能不是HAL库自带的中断函数，而是被写入startstm32f103xe.s的启动文件中。

> Remark:
> HAL库的库函数一定有HAL_前缀，没有前缀的就是自己生成或可以灵活定义的函数。

```
/**

  * @brief This function handles DMA1 channel1 global interrupt.

  */

void DMA1_Channel1_IRQHandler(void)

{

  /* USER CODE BEGIN DMA1_Channel1_IRQn 0 */

  

  /* USER CODE END DMA1_Channel1_IRQn 0 */

  HAL_DMA_IRQHandler(&hdma_adc1);

  /* USER CODE BEGIN DMA1_Channel1_IRQn 1 */

  

  /* USER CODE END DMA1_Channel1_IRQn 1 */

}
```
									代码一
```
DCD     DMA1_Channel1_IRQHandler   ; DMA1 Channel 1
```
									代码二
上述 `DCD` 指令用于在启动文件（如 `startup_stm32f1xx.s`）的**中断向量表**中登记 `DMA1_Channel1_IRQHandler` 的地址。这样，当 DMA1 通道 1 发生中断时，处理器会根据中断向量表中的记录，自动跳转到 `DMA1_Channel1_IRQHandler` 函数执行相应的中断服务。

在 `DMA1_Channel1_IRQHandler` 函数内部，通常会调用 HAL 库提供的 `HAL_DMA_IRQHandler(&hdma_adc1)` 函数。`HAL_DMA_IRQHandler` 才是真正负责处理中断逻辑的 HAL 库函数，它会识别和清除具体的 DMA 中断标志，并调用用户注册的回调函数以完成具体的数据处理任务。

```
/**

  * @brief  Handles DMA interrupt request.

  * @param  hdma: pointer to a DMA_HandleTypeDef structure that contains

  *               the configuration information for the specified DMA Channel.  

  * @retval None

  */

void HAL_DMA_IRQHandler(DMA_HandleTypeDef *hdma)

{

  uint32_t flag_it = hdma->DmaBaseAddress->ISR;

  uint32_t source_it = hdma->Instance->CCR;

  /* Half Transfer Complete Interrupt management ******************************/

  if (((flag_it & (DMA_FLAG_HT1 << hdma->ChannelIndex)) != RESET) && ((source_it & DMA_IT_HT) != RESET))

  {

    /* Disable the half transfer interrupt if the DMA mode is not CIRCULAR */

    if((hdma->Instance->CCR & DMA_CCR_CIRC) == 0U)

    {

      /* Disable the half transfer interrupt */

      __HAL_DMA_DISABLE_IT(hdma, DMA_IT_HT);

    }

    /* Clear the half transfer complete flag */

    __HAL_DMA_CLEAR_FLAG(hdma, __HAL_DMA_GET_HT_FLAG_INDEX(hdma));

  

    /* DMA peripheral state is not updated in Half Transfer */

    /* but in Transfer Complete case */

  

    if(hdma->XferHalfCpltCallback != NULL)

    {

      /* Half transfer callback */

      hdma->XferHalfCpltCallback(hdma);

    }

  }

  

  /* Transfer Complete Interrupt management ***********************************/

  else if (((flag_it & (DMA_FLAG_TC1 << hdma->ChannelIndex)) != RESET) && ((source_it & DMA_IT_TC) != RESET))

  {

    if((hdma->Instance->CCR & DMA_CCR_CIRC) == 0U)

    {

      /* Disable the transfer complete and error interrupt */

      __HAL_DMA_DISABLE_IT(hdma, DMA_IT_TE | DMA_IT_TC);  

  

      /* Change the DMA state */

      hdma->State = HAL_DMA_STATE_READY;

    }

    /* Clear the transfer complete flag */

      __HAL_DMA_CLEAR_FLAG(hdma, __HAL_DMA_GET_TC_FLAG_INDEX(hdma));

  

    /* Process Unlocked */

    __HAL_UNLOCK(hdma);

  

    if(hdma->XferCpltCallback != NULL)

    {

      /* Transfer complete callback */

      hdma->XferCpltCallback(hdma);

    }

  }

  

  /* Transfer Error Interrupt management **************************************/

  else if (( RESET != (flag_it & (DMA_FLAG_TE1 << hdma->ChannelIndex))) && (RESET != (source_it & DMA_IT_TE)))

  {

    /* When a DMA transfer error occurs */

    /* A hardware clear of its EN bits is performed */

    /* Disable ALL DMA IT */

    __HAL_DMA_DISABLE_IT(hdma, (DMA_IT_TC | DMA_IT_HT | DMA_IT_TE));

  

    /* Clear all flags */

    hdma->DmaBaseAddress->IFCR = (DMA_ISR_GIF1 << hdma->ChannelIndex);

  

    /* Update error code */

    hdma->ErrorCode = HAL_DMA_ERROR_TE;

  

    /* Change the DMA state */

    hdma->State = HAL_DMA_STATE_READY;

  

    /* Process Unlocked */

    __HAL_UNLOCK(hdma);

  

    if (hdma->XferErrorCallback != NULL)

    {

      /* Transfer error callback */

      hdma->XferErrorCallback(hdma);

    }

  }

  return;

}
```
									代码片段3

在 STM32 HAL 库中，`HAL_DMA_IRQHandler`中断服务函数会对该通道**所有相关中断标志**（如传输完成 `DMA_IT_TC`、半传输完成 `DMA_IT_HT`、传输错误 `DMA_IT_TE` 等）进行检测与处理。例如，内部会使用如下判断：

- `(source_it & DMA_IT_TC) != RESET` ：判断是否发生了传输完成中断
    
- `(source_it & DMA_IT_HT) != RESET` ：判断是否发生了半传输完成中断
    
- `(source_it & DMA_IT_TE) != RESET` ：判断是否发生了传输错误中断
    

这些中断标志的中断请求需要在 CubeMX 生成的各外设初始化函数（如 `MX_DMA_Init`、`MX_USARTx_UART_Init` 等）中**手动使能**，即通过 HAL 函数打开对应的中断源。

> **注意：**  
> 使能 DMA 通道的中断标志位，仅代表允许该通道在特定条件下产生中断请求。要确保中断能够真正响应，还需在 NVIC（嵌套向量中断控制器）中使能对应的中断通道。这一配置通常会在 CubeMX 自动生成的 `HAL_xxxx_MspInit()` 函数中完成，前提是你在 CubeMX 的 NVIC 配置页中勾选了相应通道的中断使能。

最后，针对每一个中断标志位所触发的中断，实际的业务处理逻辑应当在相应的**中断回调函数**中实现。这些回调函数通常以弱定义（`__weak`）的方式提供在对应外设的 HAL 库文件中（例如 UART 的相关回调函数定义在 `stm32f1xx_hal_uart.c` 里）。用户可根据需求，在自己的代码中重写这些回调函数，以实现自定义的中断响应操作。

```

/**

  * @brief  Rx Transfer completed callbacks.

  * @param  huart  Pointer to a UART_HandleTypeDef structure that contains

  *                the configuration information for the specified UART module.

  * @retval None

  */

__weak void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)

{

  /* Prevent unused argument(s) compilation warning */

  UNUSED(huart);

  /* NOTE: This function should not be modified, when the callback is needed,

           the HAL_UART_RxCpltCallback could be implemented in the user file

   */

}

```

在实际应用中，如果用户需要自定义外设（如 UART）的接收中断回调函数，应在对应的 `.c` 文件（如 `uart.c`）中对回调函数进行**强定义**（即重写 HAL 库中的弱定义回调函数）。

> **注意：**  
> 以 UART 为例，若希望在每次接收完成后能够反复进入接收中断，用户需在自定义的回调函数（如 `HAL_UART_RxCpltCallback`）的末尾调用 `HAL_UART_Receive_IT(&huart1, &uart_rx_buffer, 1)`，以**重新启动接收中断**。这样可以确保在接收下一个字节时，依然能够触发中断并进入回调，实现连续接收数据的功能。


## 驱动USART

驱动USART的实现步骤在上面已经讲的差不多了

关键是如果要通过串口在电脑打印信息，需要用到printf函数，这是需要 include <stdio.h>函数库，需要在Keil的Target中勾选`micro LIB`

