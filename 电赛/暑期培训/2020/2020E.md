
## 软件部分（信电）

### ADC

1. 采样方案
	ADC 时钟为 9MHz ，采样时间 71.5 时钟周期，转换时间 12.5 时钟周期，
2. 采样频率 𝑓𝑠=9𝑀𝐻𝑧/(71.5+12.5)=107.142𝑘𝐻𝑧。一次采样 4352 点，使用 DMA 传输 。






ADC采用DMA+ADC模式
按照如下方式配置
1. ADC初始化
	1. 结构体（ADC_HandleTypeDef ADC_Handle;ADC_ChannelConfTypeDef ADC_Config;）
2. DMA初始化
	1. 结构体(DMA_HandleTypeDef hdma_adcx;)


### FFT

方案： 将 ADC 采样得到的 4096 个点加上汉宁窗，再调用库函数进行快速傅里叶变
换，取绝对值后得到幅度谱。

### THD

计算方案： 在幅度谱中找到除 DC 分量外的第一个峰值，即为基频所在位置，据此



文件： dsp.c

结构
```
┌─────────────────────────────————————————┐
│               顶层应用函数                |
├────────────────────────────————————————─┤
│ float freq_u16(magBufferU)              │     // 基波频率估算
│ float vpp_q15(sampleBufferQ)            │     // 峰峰值（电压）估算
│ void  fft_q15(sampleBufferQ,fftBufferQ) │     // 窗函数+FFT变换
│ void  mag_q15(fftBufferQ,magBufferU)    │     // 幅值计算
│ float THD_to_nth(magBufferU, fft_rank)  │     // 谐波畸变THD
└────────────────────────────————————————─┘
         │       │      │
         ▼       │      │
┌────────────────────────────────────────────┐
│               中间分析/数据处理层             │
├────────────────────────────────────────────┤
│ freq_fund_index_u16(magBufferU)            │ // 找基波index（被  freq_u16/THD_to_nth调用）
│ vpp_in(bufferQ, start, bufsize)            │ // 局部峰峰值（被vpp_q15调用）
│ max_u16_around(bufferU, center, width, *result, *pos) │ // 局部最大值（被freq_fund_index_u16/THD_to_nth调用）
└────────────────────────────────────────────┘
         │         │           │
         ▼         ▼           ▼
┌─────────────────────────────────────────────────────────┐
│              底层库&数学运算/硬件相关函数                │
├─────────────────────────────────────────────────────────┤
│ arm_rfft_q15(&FFT_INSTANCE, sampleBufferQ, fftBufferQ)  │ // FFT库（被fft_q15调用）
│ sqrt(), 浮点/整数乘法等数学操作                          │ // 幅值/THD等调用
└─────────────────────────────────────────────────────────┘

```




## 方案2 （电气）

![[Pasted image 20250626192135.png]]





## 总结

1. 可以通过限制返回范围获得稳定的THD
2. 可以通过先对方波进行积分，获得三角波，再对三角波进行滤波获得稳定的正弦波。